import React from 'react';
import Title from '../'

export default class Pfc extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <Title>Расчет БЖУ</Title>           
        );
    }
}